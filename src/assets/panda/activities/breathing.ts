import type { ActivityConfig } from '@/components/panda/types';

export const breathingActivity: ActivityConfig = {
  id: 'breathing',
  preferredEmotion: 'calm',
  defaultAnimation: 'breathe',
  pose: 'default',
  armSet: 'breathing',
  accessories: ['breathing'],
  bodyScale: 1.035,
};
